<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ntu.seckill.repository.IUserRepository">

    <!-- handle column mapping -->
    <resultMap id="BaseResultMap" type="UserPO">
        <result column="registerDate" property="registerDate"></result>
    </resultMap>

    <insert id="create" parameterType="UserPO" keyColumn="id">
        <selectKey keyProperty="id" resultType="string" order="BEFORE">
            SELECT REPLACE(UUID(), '-', '') AS id FROM DUAL
        </selectKey>
        INSERT INTO `seckill.user`(id, email, name, password, salt, registerDate, phone)
        VALUES(#{id}, #{email,jdbcType=VARCHAR}, #{name},
            #{password}, #{salt}, #{registerDate,jdbcType=TIMESTAMP}, #{phone,jdbcType=BIGINT})
    </insert>
</mapper>
